python-multipart   # For handling form data (needed for login/register forms)
passlib[bcrypt]    # Password hashing library (we now use pbkdf2_sha256, but bcrypt is also there)
itsdangerous
(pip install) sqlalchemy
